const routes = {
  home: '/',
  category: '/category',
  singleAnnouncement: '/announcements/:id',
  announcements: '/announcements',
  sample: '/sample',
  register: '/register',
  login: '/login'
};

export default routes;
